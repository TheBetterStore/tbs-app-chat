AWSTemplateFormatVersion: '2010-09-09'
Description: >
  tbs-app-chat

Parameters:
  Environment:
    Type: String
    Default: prod

  AppLoginCFName:
    Type: String

  InfraBaseCFName:
    Type: String

  KmsCFStackName:
    Type: String

  EcrImageUrl:
    Type: String

  QueryLoggingLevel:
    Type: String
    Default: DEBUG
    AllowedValues: [DEBUG, INFO, WARN, ERROR, FATAL]

  BedrockModel:
    Type: String
    Default: apac.anthropic.claude-sonnet-4-5-20250514-v1:0

  MaxTokens:
    Type: String
    Default: 1024

  AllowedCorsDomains:
    Type: String
    Description: Define allowed client UI domains here. Use commas to separate if multiple.
    Default: 'http://localhost:4200,https://thebetterstore.net'

  SystemPrompt:
    Type: String
    Default: 'You are a helpful shopping assistant for The Better Store, an electronics and books retailer. Keep responses brief. Use the lookup_inventory tool to check product availability when asked about products.'

  BraveApiKeyParam:
    Type: String
    Description: SSM Parameter Store param for Brave API key

  AllowedUser:
    Type: String
    Default: 'brycepc@hotmail.com'
    Description: Allowed username for WebSocket access

Resources:

  # --- AgentCore Runtime (Option B) ---

  AgentCoreRuntimeRole:
    Type: AWS::IAM::Role
    Properties:
      Path: "/"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: bedrock-agentcore.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: !Sub ${AWS::StackName}-AgentCoreRuntimePolicies
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: BedrockPolicy
                Effect: Allow
                Action:
                  - bedrock:InvokeModel
                  - bedrock:InvokeModelWithResponseStream
                Resource: "*"
              - Sid: SsmPolicy
                Effect: Allow
                Action:
                  - ssm:GetParameter
                Resource: !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter${BraveApiKeyParam}"
              - Sid: KmsPolicy
                Effect: Allow
                Action:
                  - kms:Decrypt
                Resource:
                  Fn::ImportValue: !Sub ${KmsCFStackName}:InternalKmsKey:Arn
              - Sid: EcrPolicy
                Effect: Allow
                Action:
                  - ecr:GetDownloadUrlForLayer
                  - ecr:BatchGetImage
                  - ecr:GetAuthorizationToken
                Resource: "*"

  ChatAgentRuntime:
    Type: AWS::BedrockAgentCore::Runtime
    Properties:
      AgentRuntimeName: "tbs_app_chat_Runtime"
      Description: "TBS Chat Agent - AgentCore Runtime"
      AgentRuntimeArtifact:
        ContainerConfiguration:
          ContainerUri: !Ref EcrImageUrl
      NetworkConfiguration:
        NetworkMode: PUBLIC
      RoleArn: !GetAtt AgentCoreRuntimeRole.Arn
      ProtocolConfiguration: HTTP
      EnvironmentVariables:
        BEDROCK_MODEL: !Ref BedrockModel
        MAX_TOKENS: !Ref MaxTokens
        SYSTEM_PROMPT: !Ref SystemPrompt
        PRODUCTS_API_BASE_URL: "https://api.thebetterstore.net/product/v1"
        BRAVE_API_KEY_PARAM: !Ref BraveApiKeyParam
      LifecycleConfiguration:
        IdleRuntimeSessionTimeout: 900
        MaxLifetime: 14400
      AuthorizerConfiguration:
        CustomJWTAuthorizer:
          DiscoveryUrl:
            Fn::Join:
              - ""
              - - !Sub "https://cognito-idp.${AWS::Region}.amazonaws.com/"
                - Fn::ImportValue: !Sub ${AppLoginCFName}:UserPoolId
                - "/.well-known/openid-configuration"
          AllowedClients:
            - Fn::ImportValue: !Sub ${AppLoginCFName}:UserPoolWebClientId
      RequestHeaderConfiguration:
        RequestHeaderAllowlist:
          - Authorization

  EvaluationExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      Path: "/"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: TrustPolicyStatement
            Effect: Allow
            Principal:
              Service: bedrock-agentcore.amazonaws.com
            Action: sts:AssumeRole
            Condition:
              StringEquals:
                aws:SourceAccount: !Ref AWS::AccountId
                aws:ResourceAccount: !Ref AWS::AccountId
              ArnLike:
                aws:SourceArn:
                  - !Sub "arn:aws:bedrock-agentcore:${AWS::Region}:${AWS::AccountId}:evaluator/*"
                  - !Sub "arn:aws:bedrock-agentcore:${AWS::Region}:${AWS::AccountId}:online-evaluation-config/*"
      Policies:
        - PolicyName: !Sub ${AWS::StackName}-EvaluationExecutionPolicies
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: CloudWatchLogReadStatement
                Effect: Allow
                Action:
                  - logs:DescribeLogGroups
                  - logs:GetQueryResults
                  - logs:StartQuery
                Resource: "*"
              - Sid: CloudWatchLogWriteStatement
                Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource: !Sub "arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/bedrock-agentcore/evaluations/*"
              - Sid: CloudWatchIndexPolicyStatement
                Effect: Allow
                Action:
                  - logs:DescribeIndexPolicies
                  - logs:PutIndexPolicy
                Resource:
                  - !Sub "arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:aws/spans"
                  - !Sub "arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:aws/spans:*"
              - Sid: BedrockInvokeStatement
                Effect: Allow
                Action:
                  - bedrock:InvokeModel
                  - bedrock:InvokeModelWithResponseStream
                Resource:
                  - !Sub "arn:aws:bedrock:${AWS::Region}::foundation-model/*"
                  - !Sub "arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/*"

  ChatOnlineEvaluationConfig:
    Type: AWS::BedrockAgentCore::OnlineEvaluationConfig
    Properties:
      OnlineEvaluationConfigName: !Sub insightsmcp_${EnvironmentName}_eval
      Description: !Sub Online evaluation for ${ApplicationName} (${EnvironmentName})
      EvaluationExecutionRoleArn: !GetAtt EvaluationExecutionRole.Arn
      ExecutionStatus: ENABLED
      DataSourceConfig:
        CloudWatchLogs:
          LogGroupNames:
            - !Sub /aws/bedrock-agentcore/runtimes/${AgentCoreRuntime.AgentRuntimeId}-DEFAULT
          ServiceNames:
            - !Sub insightsmcp_${EnvironmentName}.DEFAULT
      Evaluators:
        - EvaluatorId: Builtin.GoalAttainment
        - EvaluatorId: Builtin.ResponseRelevance
        - EvaluatorId: Builtin.Harmfulness
        - EvaluatorId: Builtin.Helpfulness
        - EvaluatorId: Builtin.ToolSelectionAccuracy
        - EvaluatorId: Builtin.Faithfulness
      Rule:
        SamplingConfig:
          SamplingPercentage: !Ref EvaluationSamplingPercentage

Outputs:

  AgentCoreRuntimeArn:
    Description: AgentCore Runtime ARN
    Value: !GetAtt ChatAgentRuntime.AgentRuntimeArn


